{
  "name": "@coreyward/postgres-context-server",
  "version": "0.2.0",
  "description": "a model context protocol server for postgres; forked from @zeddotdev/postgres-context-server",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "preinstall": "bunx only-allow bun",
    "build": "bun build ./index.ts --compile --outfile .postgres-mcp-bin",
    "lint": "oxlint",
    "format": "oxlint --fix",
    "typecheck": "tsc --noEmit"
  },
  "files": [
    "index.ts"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zed-industries/postgres-context-server.git"
  },
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/zed-industries/postgres-context-server/issues"
  },
  "homepage": "https://github.com/zed-industries/postgres-context-server#readme",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.1",
    "pg": "^8.16.3",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@types/pg": "^8.16.0",
    "oxlint": "^1.41.0",
    "typescript": "^5.9.3"
  }
}
